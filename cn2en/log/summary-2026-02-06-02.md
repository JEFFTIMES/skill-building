# cn2en design + implementation update (2026-02-06)

## Changes

- Added `scripts/extract_blocks_from_json.py` to list block texts from a blocks JSON file.
- Updated `scripts/append_glossary.py` to accept batch JSON input (dict or list) instead of single-term flags.
- Updated `SKILL.md` workflow and script usage to match the new flow and glossary interface.
- Adjusted glossary append test to use the new batch interface.

## Notes

- `append_glossary.py` now requires `--source` and `--context` when the input is a JSON dict.
